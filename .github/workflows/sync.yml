name: Sync

on:
  schedule:
    - cron: '0 4 * * 3,6' # at 4am on wed & sat
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [15.x]

    steps:
      - name: ⏬ Checkout code
        uses: actions/checkout@v2

      - name: 🔢 Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: ⏬ Install
        run: |
          npm install

      - name: 🌏 Download planet file extract
        run: |
          npm run download-planet

      - name: 🥝 Download LINZ csv file
        run: |
          npm run download-linz

      - name: 🚚 Preprocess the .csv and .osm.pbf files
        run: |
          npm run preprocess -- --max-old-space-size=6144

      - name: 🔁 Conflate LINZ data with OSM
        run: |
          npm run conflate -- --max-old-space-size=6144

      - name: 🏡 Generate geojson and reports
        run: |
          npm run action -- --max-old-space-size=6144

      - name: ⛅ Upload geojson and reports to CDN
        run: |
          npm run upload -- --max-old-space-size=6144
        env:
          GH_BASIC_AUTH: ${{ secrets.GH_BASIC_AUTH }}
          AZ_CON: ${{ secrets.AZ_CON }}
